# OBSOLETE

This specification is obsolete. The container orchestration strategy has been consolidated into the other specification documents:

- The **work container** is managed by `@devcontainers/cli` as described in `02-Devcontainer-Management.md`.
- The **setup container** is managed by a custom Docker Compose file as described in `02-Devcontainer-Management.md`.
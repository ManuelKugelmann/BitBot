# Claude Flow Configuration (BitBot Dev)

This directory is mounted to `/root/.claude-flow` in the BitBot development container.

## Purpose

Persistent storage for Claude Flow configurations:
- Workflow definitions
- Automation scripts
- Build pipelines

## Mounted To

`/root/.claude-flow` (root user for dev container)

## Persistence

This folder persists across container rebuilds.

{
  "$comment": "Config template details - merged with templates/bitbot-base/.devcontainer/devcontainer.json",
  "$comment2": "bitbot-base/.devcontainer/devcontainer.json + details.devcontainer.json => devcontainer.json",
  "$comment3": "Run: templates/scripts/merge-devcontainer.sh templates/bitbot-config",

  "name": "BitBot Config Mode",
  "dockerFile": "Dockerfile",
  "workspaceFolder": "/configspace",

  "features": {
    "ghcr.io/anthropics/devcontainer-features/claude-code:1": {
      "version": "latest"
    }
  },

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode-remote.remote-containers",
        "ms-azuretools.vscode-docker",
        "redhat.vscode-yaml"
      ],
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash",
        "files.associations": {
          "devcontainer.json": "jsonc",
          "Dockerfile": "dockerfile"
        }
      }
    }
  },

  "$comment_config_mounts": "Config mode: Mount configspace root + workspace subdirectory",
  "mounts": [
    "source=${localWorkspaceFolder},target=/configspace,type=bind,consistency=cached",
    "source=${localWorkspaceFolder},target=/configspace/workspace,type=bind,consistency=cached",
    "source=${localWorkspaceFolder}/.devcontainer,target=/configspace/workspace/.devcontainer,type=bind,consistency=cached"
  ],

  "postCreateCommand": "npm install -g @devcontainers/cli && echo '✓ DevContainer CLI installed'"
}
